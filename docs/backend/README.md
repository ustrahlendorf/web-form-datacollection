# backend

This folder documents the **Vis-Connect backend** — a standalone Python package for interacting with the Viessmann IoT API. It is separate from the main web form application (API Gateway, Lambda, DynamoDB).

## contents

- `vis-connect.md` — Viessmann Vis-Connect: OAuth, IoT config, feature fetching, CLI tools
