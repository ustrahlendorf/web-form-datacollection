# web-form-verbrauch

AWS-hosted web form and data pipeline for heating/consumption workflows (CDK, Lambda, frontend).

- **Docs:** [Getting started](docs/getting-started.md) is the main guide; see [docs/README.md](docs/README.md) for the full documentation index.
- **Python (CDK, pytest, tooling):** `python3 -m venv .venv && .venv/bin/pip install -r requirements.txt`
