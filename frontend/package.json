{
  "name": "data-collection-frontend",
  "version": "1.0.0",
  "description": "Frontend for Data Collection Web Application",
  "main": "src/app.js",
  "scripts": {
    "test": "jest",
    "build": "bash build.sh",
    "serve": "npm run build && python3 -m http.server 8000 --directory build",
    "deploy": "npm run build && aws s3 sync build/ s3://$S3_BUCKET --delete"
  },
  "keywords": [
    "data-collection",
    "aws",
    "cognito",
    "serverless"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "fast-check": "^3.15.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^30.2.0"
  }
}
